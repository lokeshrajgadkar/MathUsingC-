#include <iostream>
using namespace std;

int main()
{
    
    float a,b,c; 
    cout << "Enter 1st no: ";
    cin >> a;
    cout << "Enter 2nd no: ";
    cin >> b;

    // multiplication of two numbers in stored in variable c
    c = a * b;

    // Prints reslut 
    cout << a << " * " <<  b << " = " << c;     

    return 0;
}
